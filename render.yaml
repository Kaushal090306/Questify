# Render Blueprint for Django + React Monorepo

services:
  - type: web
    name: questify-backend
    env: python
    region: oregon
    buildCommand: |
      echo "Python version: $(python --version)"
      echo "Current directory: $(pwd)"
      echo "Available files: $(ls -la)"
      pip install --upgrade pip setuptools wheel
      echo "Installing requirements..."
      pip install -r requirements-stable.txt
      echo "Collecting static files..."
      python manage.py collectstatic --noinput
      echo "Build complete!"
    startCommand: bash prestart.sh && gunicorn backend.wsgi:application
    envVars:
      - key: DJANGO_SECRET_KEY
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: questify-db
          property: connectionString
      - key: ALLOWED_HOSTS
        value: .onrender.com
      - key: GOOGLE_GEMINI_API_KEY
        sync: false
      - key: DEBUG
        value: "False"
    plan: free

  - type: web
    name: questify-frontend
    env: static
    region: oregon
    buildCommand: |
      cd frontend
      npm install
      npm run build
    staticPublishPath: frontend/build
    envVars:
      - key: REACT_APP_SOCKETIO_URL
        value: https://questify-backend-cl4v.onrender.com
      - key: REACT_APP_API_URL
        value: https://questify-backend-cl4v.onrender.com/api
      - key: REACT_APP_WS_URL
        value: wss://questify-backend-cl4v.onrender.com
    plan: free

databases:
  - name: questify-db
    region: oregon
    plan: free
    postgresMajorVersion: 15
